# 對齊鎖

十二席。唔加第十三席。唔估米。唔新造 mesher 或 rigger。唔 commit。唔停正在跑嘅 produce。

## 分鏡席

`runBoards` 而家只 `chatJson`，寫場所、動作、秒數。`boards.SC01`／`SC02`／`SC03` 係文字收據，唔係圖。文字 callsheet 唔算分鏡做完。`stills/trial16` 同 `trial16b` 唔係呢份片嘅鍵格。

分鏡席要交可見分鏡板、切出嘅格，同埋格屬於邊鏡、鏡入面邊個位。出板、切格、抽壞格、補格都經 `runBoards`，收據留喺分鏡席。`storyboardBoardPrompt` 要喺呢條路上面。

十一鏡唔等於十六格。16 同 48 只係思考用嘅切格數，幾多格都得。一次 U1.5 切一板。壞格抽出，再併成 16 格交回 U1.5，或者併成 8 或 4。補格只換指定嗰格。空格唔製造新鏡。

## 兩條 H3 輸入，唔好互稱

鍵格百分比：`H3Keyframes`。`positions` 係百分比。`image_1` 對第一個位，之後順序接。`image_1` 至 `image_6` 只係最先六個釘，唔係上限。第七張起入 `images_batch`（而家未接）。圖釘喺該鏡 `17k+5` 總長度上。H3 只生成兩個釘之間嘅幀。冇 Video 1 先係呢條路。

參考圖：樣貌同身份，冇百分比。有灰色走位片、零鍵格，先係 C 形：`ref_images.ref_image_0` 係角度肖像，Video 1 只交動作。故事鏡唔把場景相塞入 `ref_images`。多鏡一次出只收未切身份板，拒收走位片同鍵格名。

冇寫百分比就同時塞鍵格同走位片，會拒。寫咗百分比，型別註解話可以同圖，§5b 話會雙重曝光。兩句都喺，未判定邊句贏。唔好自己揀邊句贏。

## 場景唔係樓

`buildings` 空就唔造樓。廚房、露台係房入面嘅畫面，唔係「一幢完整建築，由基座到屋頂」。木檯唔係木屋。`scene-廚房.png`、`scene-露台.png`、`scene-木檯.png` 係建築板零參考生成，退出鍵格參考鏈。

## 聲

AuK `:9882`。`/tts` 只係照參考聲讀一句對白。情緒符號會被讀出聲，唔好塞入對白。`/run` 先至係分類之後嘅其他任務（情緒、快慢、音高、非語言等），廠未接。SenseVoice `:9881` 係 AuK 參考音同聲檢嘅辨認。成片大部分靜音時，聲檢對完整旁白稿會 CER 失敗；呢個唔等於兩句對白讀錯。靜音聲軌要跟鏡長，唔好再寫 0.1 秒。聲喺成片頁聽得到，唔係畫布節點。

## 畫布同網頁

一集一塊畫布。分鏡格、角色轉面、場景、道具、鍵格係同一層節點。剪接線只連鍵格。參考線另設。縮圖用小預覽，撳開先至係大圖。而家每張 `SHxx.png` 十多 MB 直接當縮圖，所以每格都要等。

## H3 幀數

八段片實檔 577 幀。閘用每鏡 `17k+5` 加總係 533。提交寫過每鏡 68 幀（八鏡 544）。已對齊網格嘅預算係每鏡 73 幀（八鏡 584）。唔好把閘改成 577 就當解決。

## 你自己做

讀呢份同廠入面矛盾嘅代碼、席位、技能。你覺得同呢份鎖唔符嘅，自己睇、自己改。唔好問。唔好 commit。唔好停正在跑嘅 produce。
