# CARD_SYSAVATAR_QC_0920 — onto-seats 重放（Planner，0083629 MERGE_OK）

來源：lane/qc `0083629`（Vera MERGE_OK 六點）。seats 已有 cookbook /edit（BUG3）＋ hopStillPlans（BUG4）——**唔 cherry-pick**，手重放淨效應。

改：`propNounClass` 加 `system`（光框／全息／infograph 先於 garment/document）；cookbook 【道具】三層句（圖表+UI+文字，零犁／零 screen／零 forbid echo）；`keyframeRequire` system 開 prop 閘；`judge()` screen 族豁免（system 免死、非 system 照殺、螢幕↔screen）＋混合形 people_count（只多頭＋投影／全息／縮細字）；trace `wantTemplate` system off、`expectedKeys` 鏡射 keyframeRequire（SH09 轉綠）。

seats 適應：src 唔寫實體 token「系統」（noun-lint）；SYSTEM_RE 用 光框／全息／infograph 等同族。上游 boards screen-forbid strip 留喺未 commit WIP，唔入呢個 commit。
