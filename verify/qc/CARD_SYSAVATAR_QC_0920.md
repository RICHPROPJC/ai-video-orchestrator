# CARD_SYSAVATAR_QC_0920 — §0c 系統形象法 photo-qc 實現（WR1Q SH02 root cause）

改咩：keyframe-prompt.ts propNounClass 加「system」類（光框/光幕/全息/投影/infograph/信息圖/系統/hologram，先於 garment/document 判類）＋ keyframeEditPrompt 加 system 三層句（圖表+UI框+文字標籤，零犁詞、零 screen 字、零 forbid echo）＋ keyframeRequire 照開 prop 閘；photo-qc.ts judge() 加 systemProp 訊號（require.tool 再分類，stale require.json 都食）——tool_forbid 檢查 screen 族 token（screen/螢幕/屏幕/萤幕/荧幕）喺 system prop 豁免、非 system 照殺（螢幕↔screen 同族互match），people_count 閘混合形容差（只 system shot、只多頭、盲眼要寫到 投影/全息/縮細/系統人樣 先放，checks.people_count_system_form 做記號）；trace.ts wantTemplate system→template off、checkKeyframeToStills wantTool 含 system＋expectedKeys 改為跟 shot 鏡射 keyframeRequire（修埋 pre-existing gap：location/action/size 上 require 就 false drift，SH09 fixture 就係咁先轉綠）。
點驗：bun direct keyframe-prompt.test.ts 8/8、trace.test.ts 8/8（含新 §0c 鎖：①system prop 盲寫「全息螢幕」GREEN＋non-screen forbid（book）照 FAIL②非 system prop 盲寫「螢幕」FAIL 於 screen 族③混合形 縮細系統人樣 GREEN＋冇投影字 FAIL＋非系統多一人 FAIL＋少人 FAIL）；tsx --test --test-force-exit photo-qc.test.ts 61/61 EXIT=0（舊 56＋新 5）。
結果：全 studio suite 282 ok／8 fail，fail 名單同 HEAD baseline 逐一 diff 一致（4 workbench blockout render＋1 doctor fleet＋1 pipeline resume＋2 crew trials，全部 pre-existing、唔屬本卡）；baseline 9 fail 入面 SH09 paper fixture 經 expectedKeys 修復轉綠，本卡零新增 fail；tsc --noEmit 21 行錯 HEAD 前後不變（pipeline.ts loadBaseCast 等 pre-existing，我改動檔案零錯）。commit lane/c8a，無推 github。
