# SF3D_WIRE_B — 0919 P1卡B：SF3D接廠（Fable5.1經SlateLead派）

**做咗**：SF3D由資產線變工廠內建——新 `src/lib/studio/mesh-provider.ts`（plate 去背方塊閘 → POST `sf3d_server :8018` `/generate`，server 自帶 canonicalize 直接收，client 永不重做；GLB header 驗證；fresh out_dir 唔准重用；mesh-receipt.json 連 sha256 鏈）＋ `src/lib/studio/mesh-geometry-gate.ts`（Blender **只准5.1.2** 版本閘；astra `object.import_mesh` 同款數學：glTF直入→-90°X拆glTF Y-up→heightCalibrated 1.7m米契約；solidity=volume/convex-hull（trimesh定義）＋thinness＋Cycles CPU render preview 三查；figure帶[0.35,0.80] anchor hero 0.583／prop帶[0.08,0.75] anchor chair 0.298）＋ `mesher` config段（endpoint/blender/textureResolution/targetHeightM，env `SF3D_URL`）＋ CLI `mesh` 指令＋doctor sf3d probe。**真跑A/B**：`hero_precut.png`（D1去背板）→ server（10.89s, peak 6102MB, canonicalized:true）→ gate 綠：solidity **0.631** figure級、thinness 0.253、verts 6510、dimensionsM 0.455×0.431×**1.7000**（直立證據）、preview luma span 60；收據 `tripo_assets/photo_film_test/char_loop/codex_sf3d_factory_wire_b_0919_r2/`（mesh-receipt + gate_receipt + preview.png，source_sha256 全鏈對得上）。另 hookaudit `ASSET_STOCK_0918.md` 已補標：0919 05:00前所有SF3D mesh INVALID（cu_seqlens blob案），batch名全列。

**Proof**：tests 25/25綠（`tsx --test mesh-provider mesh-geometry-gate doctor`）；全套 257 test 249 pass，7 fail 全係 HEAD 原有（blockout WORKBENCH render——本機GPU2滿載下SIGSEGV，我手動證實 blockout式render一樣炸；pipeline/reflector fetch），stash對照一致；`tsc --noEmit` 我檔案零錯（全廠17錯係原有）；live `/health` `{ready:true,served:1}` 前後無停過服務。收據目錄：`/mnt/ssd/tripo_assets/photo_film_test/char_loop/codex_sf3d_factory_wire_b_0919_r2/{mesh-receipt.json,0/mesh_front.glb,0/canonical_receipt.json,gate/gate_receipt.json,gate/preview.png}`。

**未做**：①pipeline.ts blockout render 未改食 mesh（mannequin照舊——要plates/turnaround流程先有意義，另卡）；②blob solidity實測值未有（舊blob批唔重跑GPU，帶上限0.75/0.80係按「lump≈凸」推理，第一次真blob過閘時要驗譛條帶）；③WORKBENCH render喺GPU2滿載時SIGSEGV係全廠既有問題（blockout同炸），gate已用Cycles CPU免疫，但blockout條線要另開卡；④e9 watcher舊RGB crop線未遷移（工廠線只收去背方塊，RGB板會被閘拒）。
