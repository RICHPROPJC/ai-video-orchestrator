# CARD_D — 0919 P2卡D：search-first PE接pipeline四掣（SlateLead派，法源CHAU_FULL_FLOW_LAW_0917 §1）

**做咗**：四掣落齊。掣1 packet facts——`boards-contract.ts` `require.facts:{claim,source,fetched_at}[]`（同 negatives 同形 packet-authored；fetched_at 鎖 YYYY-MM-DD；另有 `require.factsRequired` 席位顯式標記），`types.ts` `ShotFact/ShotRequire` 入 Shot 鏈，`boards-expand.ts` 逐字穿線（code只組裝唔作者）。掣2 拒出閘——`keyframe-prompt.ts` `needsShotFacts`（TEXT_SCREEN_RE 結構詞彙同 propNounClass 一register＋factsRequired flag）＋`keyframeEditPrompt` 開位即閘：文字圖/infographic shot 冇 facts → `facts_missing: …文字圖要facts，去PE步攙…`（prompt_too_thin 嘅形，fail-loud 唔交電報）；facts 有齊時 `factsBlock` 逐字組裝入 prompt（【上屏事實】＋claim/source/fetched_at）。掣3 PE步——新 `pe-step.ts`：wigolo（`/mnt/ssd/u1_canvas_research/wigolo_research.py` quick depth，NDJSON MCP客戶端）→PE腦出 `{facts,render}`（render＝Image PE Render JSON object），路由照法：nex `:8017` `nex-n2.5`（chat_template_kwargs.reasoning_effort=none＋官方sampling 0.7/0.95/40＋max_tokens≥5000有閘）首選、qwen38 `:8015` 後備（plain sampling）、GLM/deepseek 腦 config 即拒（0919 thinking爆content法）；finish=length 截斷即棄唔硬parse；facts 過濾至 render 實際上屏嗰啲（≤8，zod上限；wire實證PE會倒成個證據表45行，盲眼唔可能echo）；pipeline.ts 喺 stillPlans 前接線：!dryRun 時 needsShotFacts 冇facts嘅shot行PE步→facts寫入 `shot.require.facts`→render 以【螢幕畫面 Render】段附上；收據 `stills/<SH>.pe_step.json`。掣4 判官——`photo-qc.ts` `QcRequire.facts`＋`factTokens`（日期先lift→數字連,./%→剝晒日期數字先攞CJK名核≥3字，「今年2021年9月」slip即FAIL），judge 對照對象由任務原文擴到facts原文；BLIND_PROMPT/SUMMARIZE_PROMPT 焊死零改動。config `pe` 段＋slatecrew.config.json（endpoint/model/fallback/maxTokens 6000/wigoloClient/timeoutMs）。

**Proof**：tests——`pe-step.test.ts` 7/7（mock wigolo fixture＋mock fetchImpl：nex wire call-shape逐欄斷言、後備路徑、GLM拒、雙腳零facts即throw、facts過濾、validateFacts、max_tokens閘）；`keyframe-prompt.test.ts` 12/12（拒出閘三形：action詞/flag/prop名；facts逐字組裝；require穿線）；`photo-qc.test.ts` 16/16（factTokens原子、GREEN、錯數FAIL、年slip FAIL、require穿線）；`boards-contract.test.ts` 4/4（zod schema＋fetched_at閘＋空source拒＋omittable）；pipeline/boards-expand 18 pass 0 fail。全套 studio：blockout 4 fail＋reflector 2 fail＝HEAD原有（stash對照一致），零新regression；`tsc --noEmit` 17錯全係原有類別（drama/episode/loadBaseCast/kept/bun:test/LayoutProps），我檔零新錯。**Live A/B三次全機印**（`verify/crew/pe_live_ab.json`＝末次nex收據）：①nex-n2.5 render-string版炸JSON（5478位）→改render-object形；②qwen38後備一發中（8 facts全paint、真來源差估署/中原/美聯、fetched_at 2026-09-18）；③nex-n2.5主腦直接通（81.7s wigolo＋PE，5 facts對應render visible_text逐字：321.5/323/-0.46%/288.1/+11.59%）——兩個PE腦都有live實證，路由法落死。

**未做**：①`seat-charters.ts` BOARDS_CHARTER 未加 facts 欄教學句——檔案有第啲人未提交改動（EVIDENCE_LAW句），唔溝埋；阿圖 pre-author 路徑靠 zod message 自教，生產主路徑係 PE 步運行時寫入，唔靠 charter。②doctor 未加 pe probe（pe 段有 config，驗收冇要求；可另開細卡）。③pipeline PE 步只喺 !dryRun 行：dry-run 遇文字圖冇facts會 facts_missing 拒出（refuse-to-emit 設計如此，收據要嘅話 packet 先 author facts）。④facts 寫入係運行時進 `shot.require`（Shot 鏈冇 zod 重驗，上限靠 pe-step slice(0,8)）；boards seat 自己 author 嘅 facts 先過 zod max(8)。⑤手機畫面 ref 位（Image-2/Image-3 邊個位）係 front lane 卡③b UI photo 通道嘅範圍，呢張卡唔掂。
