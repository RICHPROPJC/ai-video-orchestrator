# CARD_BUG4_0920 — QC逐鏡即判（工程路）receipt (Ivo, 2026-09-20)

- **改咗**：`pipeline.ts` runPhotoQc 由 stills 迴圈後嘅獨立 stage 搬入迴圈內（每張 still /edit 完即 geometry 預檢＋盲測，GREEN pin 先存在，下一鏡 /edit 先攞到佢做 Image-2）；T44 GREEN-only 閘原封不動——image ref 同肖像一律 `pinQcAccepted` 先准入，剷走 df762db 添嘅「本輪 stills 鏈」bypass（嗰條係未 GREEN 就入 refs，正正違反 T44）；retry 路照舊（location FAIL 退阿圖重寫一次＋通用補 prompt 重 /edit 一次，兩次唔過 blocked）。未有行 PASS_UNCONFIRMED 弱化路。副帶：845b9cd 幽靈 `loadBaseCast` 補真身（keyframe-prompt.ts 讀 `projects/<drama>/base/cast.json`，冇檔＝冇 override——唔補 bun 連 `./pipeline` 都 link 唔到，pipeline test 全軍唔入）。
- **點驗**：新 `src/lib/studio/pipeline-chain-qc.test.ts` 兩鏡序列（真 `pinQcAccepted`、真 stills 迴圈，淨係 mock HTTP 四條 lane：U1.5 /edit／ssh／MARS／sov-data bus）——GREEN 路斷言 SH02 嘅 /edit refs 包含 `stills/SH01.png` 而且 `image:SH01` 冇被 ref_rejected、事件序「SH01 GREEN」先過「SH02 /edit 完成」；FAIL 路斷言 SH01 兩次唔過即 blocked、SH02 永遠冇 /edit、冇 still 做過 ref。既有測試：`bun src/lib/studio/pipeline.test.ts` 8/8、`bun src/lib/studio/keyframe-prompt.test.ts` 23/23、`npx tsx --test src/lib/studio/*.test.ts` 261 pass／3 fail／11 skip——嗲 3 個 fail（doctor 1＋reflector 2）已用 stash 走我改動重跑核實係基底本紅，與 bug4 無關；`tsc --noEmit` 18 錯全屬基底（drama/episode L1b 半落地等），ghost `loadBaseCast` TS2305 已消失、零新錯。
- **結果**：commit `981f33a`（lane/front，5 files，+429/−54），未 push（卡禁），crew-seats/stash@{0} 冇掂，服務一個冇停。WR1Q 嗰種「每鏡 ref_rejected」結構死局解除：連戲鏈第一 pass 而家結構上行得通，GREEN-only 法律一隻字冇改。
